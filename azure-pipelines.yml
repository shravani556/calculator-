# Test pipeline
trigger:
- main

pool:
  name: Default

steps:
- script: |
    echo "Agent Directory:"
    cd
    echo "Python Version:"
    python --version
    echo "Python Location:"
    where python
  displayName: 'Verify Setup'